import sys

if len(sys.argv) < 2:
    print("No arguments provided, exiting...")
    # sys.exit(1)  # Exit with a non-zero status to indicate an error
else:
    print("Arguments provided, continuing...")
